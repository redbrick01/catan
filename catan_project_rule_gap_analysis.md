# 카탄 프로젝트 규칙 보완 문서

작성일: 2026-05-26  
기준 규칙 문서: `catan_detailed_rules.md`  
비교 대상 프로젝트 파일: `index.html`, `script.js`, `styles.css`, `server.js`

## 1. 현재 구현 요약

현재 프로젝트는 단일 HTML/CSS/JavaScript 기반의 브라우저 카탄 게임이다.

이미 구현된 주요 기능은 다음과 같다.

- 3~4인 플레이어 설정
- 기본 카탄 보드 랜덤 생성
- 사막 및 도둑 초기 배치
- 초기 정착지와 도로 배치
- 두 번째 초기 정착지의 시작 자원 지급
- 주사위 굴림과 자원 생산
- 도둑이 있는 지형의 생산 차단
- 7이 나왔을 때 도둑 이동
- 도로, 정착지, 도시 건설
- 건설 비용 차감
- 개발 카드 더미 구성
- 개발 카드 구매
- 기사 카드 사용
- 최대 기사력 보너스
- 최장 교역로 보너스
- 은행 4:1 교환
- 10점 승리 판정

프로젝트는 기본적인 플레이 흐름을 갖추고 있으나, `catan_detailed_rules.md` 기준으로 보면 몇 가지 핵심 규칙이 간소화되어 있거나 아직 구현되지 않았다.

## 2. 1단계: 규칙 정확도 우선 수정

가장 먼저 실제 규칙과 다르게 동작할 가능성이 큰 부분을 수정한다.

### 2.1 한글 인코딩 및 표시 문구 점검

대상 파일:

- `index.html`
- `script.js`
- `styles.css`

일부 텍스트가 깨져 보이는 현상이 있다. 브라우저에서도 깨진다면 사용자 경험에 직접 영향을 주므로 우선 수정해야 한다.

점검 항목:

- 페이지 제목
- 버튼 텍스트
- 안내 문구
- 로그 메시지
- 자원 이름
- 플레이어 정보 표시

### 2.2 도로 건설 연결 규칙 보완

대상 함수:

- `canBuildRoad()` in `script.js`

현재 도로 건설 가능 여부는 자신의 도로 또는 건물과 연결되어 있는지를 중심으로 판단한다. 하지만 카탄 규칙상 상대 정착지나 도시가 있는 교차점은 내 도로망을 이어 주는 연결점으로 사용할 수 없다.

보완 내용:

- 새 도로가 기존 내 도로와 연결될 때, 연결 교차점에 상대 건물이 있으면 연결로 인정하지 않는다.
- 내 정착지나 도시는 연결점으로 사용할 수 있다.
- 빈 교차점도 내 도로끼리 연결되는 지점으로 사용할 수 있다.

### 2.3 최장 교역로 계산 보완

대상 함수:

- `longestRoadLength()` in `script.js`
- `updateLongestRoad()` in `script.js`

현재 최장 교역로 계산은 내 도로 그래프를 DFS로 탐색한다. 그러나 상대 정착지나 도시가 내 도로 중간에 놓인 경우, 그 지점에서 도로가 끊기는 규칙을 반영해야 한다.

보완 내용:

- DFS 탐색 중 현재 교차점에 상대 건물이 있으면 그 지점 너머로 진행하지 않는다.
- 상대가 내 도로 중간에 정착지를 세운 경우 최장 교역로를 다시 계산한다.
- 동률일 때 기존 보유자가 유지되는 규칙을 명확히 반영한다.
- 기존 보유자가 없고 동률만 발생한 경우 아무도 보너스를 받지 않도록 처리한다.

### 2.4 7 카드 버리기 선택 UI 추가

대상 함수:

- `discardForSeven()` in `script.js`

현재는 8장 이상 보유자가 버릴 자원을 자동으로 선택한다. 실제 규칙에서는 해당 플레이어가 직접 보유 카드의 절반을 선택해 버려야 한다.

보완 내용:

- 7이 나오면 자원 8장 이상 보유자를 찾는다.
- 각 대상 플레이어별로 버릴 자원을 직접 선택하는 UI를 표시한다.
- 버려야 할 수량과 현재 선택 수량을 표시한다.
- 모든 대상자가 버리기를 완료한 뒤 도둑 이동 단계로 넘어간다.

### 2.5 도둑 약탈 대상 선택 UI 추가

대상 함수:

- `moveRobber()` in `script.js`
- `stealRandom()` in `script.js`

현재 도둑을 옮긴 지형에 약탈 가능한 상대가 여러 명 있으면 첫 번째 대상에게서 자동으로 가져온다. 실제 규칙에서는 현재 플레이어가 인접한 상대 중 한 명을 선택한다.

보완 내용:

- 도둑 이동 후 인접한 상대 목록을 만든다.
- 자원을 가진 상대만 선택 대상으로 표시한다.
- 대상자가 여러 명이면 선택 UI를 띄운다.
- 선택된 상대에게서 자원 1장을 무작위로 가져온다.
- 대상자가 없으면 바로 다음 단계로 넘어간다.

## 3. 2단계: 턴과 행동 제한 정교화

### 3.1 건설, 교환, 개발 카드 구매 타이밍 제한

대상 함수:

- `buyDevCard()` in `script.js`
- `bankTrade()` in `script.js`
- `renderControls()` in `script.js`

카탄에서는 일반적으로 주사위를 굴린 뒤 교역과 건설을 진행한다. 현재 구현에서는 일부 버튼이나 함수가 주사위 전에도 실행될 가능성이 있다.

보완 내용:

- 게임 단계가 `play`가 아니면 실행 불가
- 승리자가 있으면 실행 불가
- 주사위를 굴리기 전에는 건설, 교환, 개발 카드 구매 불가
- 도둑 처리 중에는 다른 행동 불가
- 버튼 비활성화뿐 아니라 함수 내부에서도 조건 검사

### 3.2 한 턴에 개발 카드 1장 사용 제한

대상 함수:

- `playKnight()` in `script.js`
- 향후 추가될 개발 카드 사용 함수들

현재는 한 턴에 개발 카드를 이미 사용했는지 추적하지 않는다.

보완 내용:

- 게임 상태에 `usedDevThisTurn` 같은 플래그 추가
- 개발 카드 사용 시 플래그를 `true`로 설정
- 턴 종료 시 플래그 초기화
- 승점 카드는 일반 사용 카드가 아니므로 별도 처리

### 3.3 새로 산 개발 카드 판정 개선

대상 데이터:

- `player.dev`
- `player.newDev`

현재 새로 산 개발 카드는 카드 이름 문자열로 추적한다. 같은 종류의 오래된 카드와 새 카드가 함께 있으면 정확한 판정이 어려울 수 있다.

보완 내용:

- 개발 카드를 문자열 대신 객체로 저장한다.
- 예시 구조:

```js
{
  id: "dev-12",
  type: "knight",
  boughtTurn: 8,
  used: false
}
```

- 현재 턴에 구매한 카드인지 `boughtTurn`으로 판정한다.
- 사용한 기사는 별도 공개 영역 또는 `used: true` 상태로 관리한다.

## 4. 3단계: 개발 카드 전체 구현

대상 함수:

- `makeDevDeck()` in `script.js`
- `buyDevCard()` in `script.js`
- `playKnight()` in `script.js`
- 신규 개발 카드 사용 함수

현재 개발 카드 더미에는 모든 기본 카드가 들어 있지만, 실제 사용 가능한 카드는 기사 카드뿐이다.

### 4.1 도로 건설 카드

규칙:

- 도로 2개를 무료로 놓는다.
- 일반 도로 배치 규칙은 그대로 적용한다.
- 도로 말이 부족하면 가능한 만큼만 처리하거나 사용을 막아야 한다.

구현 방향:

- `selectedAction` 또는 별도 상태를 `roadBuilding`으로 설정
- 무료 도로 배치 횟수 2회를 추적
- 배치 완료 후 일반 행동 상태로 복귀

### 4.2 풍년 카드

규칙:

- 은행에서 원하는 자원 2장을 가져온다.
- 같은 자원 2장 또는 서로 다른 자원 2장을 선택할 수 있다.

구현 방향:

- 자원 2개 선택 UI 추가
- 은행 재고 시스템을 추가한다면 재고도 확인
- 선택 완료 후 자원 지급

### 4.3 독점 카드

규칙:

- 자원 종류 하나를 선언한다.
- 다른 모든 플레이어는 해당 자원을 전부 사용한 플레이어에게 넘긴다.

구현 방향:

- 자원 선택 UI 추가
- 선택된 자원에 대해 다른 플레이어 보유량 합산
- 해당 자원을 모두 현재 플레이어에게 이동
- 로그에 획득 총량 표시

### 4.4 승점 카드

규칙:

- 숨겨 두었다가 승리 조건을 만족할 때 공개할 수 있다.
- 일반 개발 카드 사용 제한과 다르게 취급한다.

현재 이슈:

- `visiblePoints()`가 승점 카드를 바로 점수에 포함할 수 있다.
- 이 경우 상대에게 숨겨야 할 승점 정보가 노출된다.

보완 내용:

- 공개 점수와 실제 점수를 분리한다.
- 다른 플레이어에게는 승점 카드를 제외한 공개 점수만 표시한다.
- 현재 플레이어에게는 자기 실제 점수를 보여줄지 선택한다.
- 승리 판정은 실제 점수를 기준으로 처리한다.

## 5. 4단계: 교역 시스템 확장

현재 구현된 교역은 은행 4:1 교환뿐이다. 규칙 문서 기준으로는 플레이어 간 교역과 항구 교역이 추가되어야 한다.

### 5.1 플레이어 간 교역

구현 방향:

- 현재 턴 플레이어가 제안 생성
- 줄 자원과 받을 자원 선택
- 대상 플레이어 또는 전체 플레이어에게 제안
- 상대가 수락하면 양쪽 자원 보유량 검증 후 교환
- 자기 턴이 아닌 플레이어끼리의 교역은 금지

### 5.2 항구 데이터 추가

필요 데이터:

- 항구 위치
- 항구 종류
- 항구에 연결된 교차점

항구 종류:

- 3:1 일반 항구
- 목재 2:1
- 벽돌 2:1
- 양 2:1
- 밀 2:1
- 철 2:1

### 5.3 항구 교역 구현

구현 방향:

- 플레이어가 항구에 인접한 정착지나 도시를 가지고 있는지 판정
- 가능한 교환 비율 목록을 UI에 표시
- 특정 자원 2:1 항구는 해당 자원만 2장으로 교환 가능
- 일반 3:1 항구는 같은 자원 3장으로 교환 가능
- 기본 4:1 교환은 항상 가능

## 6. 5단계: 은행 자원 재고 시스템

현재 자원은 플레이어 숫자 값만 관리한다. 규칙 문서의 자원 부족 처리를 반영하려면 은행 재고가 필요하다.

### 6.1 은행 재고 추가

기본 자원 카드 수량을 상태로 관리한다.

예시:

```js
bank: {
  forest: 19,
  field: 19,
  pasture: 19,
  hill: 19,
  mountain: 19
}
```

### 6.2 자원 생산 시 재고 확인

구현 방향:

- 생산 전 자원 종류별 필요 총량 계산
- 은행 재고가 충분하면 지급
- 부족하면 해당 자원은 아무도 받지 못하게 처리
- 일부 플레이어에게만 지급하지 않는다.

### 6.3 자원 반환 처리

은행 재고를 도입하면 다음 상황에서 은행으로 자원이 돌아가야 한다.

- 건설 비용 지불
- 개발 카드 구매
- 은행 교역
- 항구 교역
- 7 규칙으로 카드 버리기
- 독점 카드로 이동하는 자원은 은행을 거치지 않음

## 7. 6단계: UI와 플레이 편의성 개선

### 7.1 개발 카드 패널

추가할 내용:

- 현재 플레이어의 개발 카드 목록
- 사용 가능한 카드와 사용 불가능한 카드 구분
- 이번 턴에 구매한 카드 표시
- 기사 공개 수 표시

### 7.2 비용표 패널

추가할 내용:

- 도로: 목재 1, 벽돌 1
- 정착지: 목재 1, 벽돌 1, 양 1, 밀 1
- 도시: 밀 2, 철 3
- 개발 카드: 양 1, 밀 1, 철 1

### 7.3 턴 단계 표시

현재 안내 문구는 있으나, 게임 상태를 더 명확히 보여주면 좋다.

표시할 단계:

- 초기 배치
- 주사위 전
- 자원 생산 완료
- 7 카드 버리기
- 도둑 이동
- 약탈 대상 선택
- 건설 및 교역
- 게임 종료

### 7.4 선택 모달 또는 패널

다음 상황에서는 명시적인 선택 UI가 필요하다.

- 7 규칙으로 버릴 자원 선택
- 도둑 약탈 대상 선택
- 풍년 카드 자원 선택
- 독점 카드 자원 선택
- 플레이어 간 교역 제안/수락

## 8. 추천 구현 순서

전체 보완 작업은 다음 순서로 진행하는 것을 추천한다.

1. 한글 깨짐 및 표시 문구 정리
2. 도로 건설 연결 규칙 수정
3. 최장 교역로 계산 보완
4. 7 카드 버리기 선택 UI 추가
5. 도둑 약탈 대상 선택 UI 추가
6. 건설, 교환, 개발 카드 구매 타이밍 제한
7. 한 턴에 개발 카드 1장 사용 제한 추가
8. 개발 카드 데이터 구조 개선
9. 도로 건설 카드 구현
10. 풍년 카드 구현
11. 독점 카드 구현
12. 승점 카드 비공개 점수 처리
13. 플레이어 간 교역 구현
14. 항구 데이터와 항구 UI 추가
15. 3:1 및 2:1 항구 교역 구현
16. 은행 자원 재고 시스템 추가
17. 비용표, 개발 카드 패널, 턴 단계 UI 개선

## 9. 우선순위 요약

### 필수 수정

- 한글 깨짐
- 도로 연결 규칙
- 최장 교역로 판정
- 7 카드 버리기 선택
- 도둑 약탈 대상 선택
- 행동 타이밍 제한

### 핵심 추가

- 개발 카드 전체 구현
- 승점 카드 비공개 처리
- 플레이어 간 교역
- 항구 교역

### 완성도 향상

- 은행 재고
- 개발 카드 패널
- 비용표
- 턴 단계 표시
- 선택 모달

