# 카탄 프로젝트 개발 프로세스 가이드라인

작성일: 2026-05-26  
적용 범위: 카탄 프로젝트의 규칙 보완, UI 개선, 버그 수정, 테스트 보강, 온라인 모드 구현 전체

## 1. 목적

이 문서는 카탄 프로젝트에서 개발 작업을 진행할 때 반드시 따라야 하는 공통 절차를 정의한다.

이 프로젝트는 단일 브라우저 오프라인 게임에서 시작했지만, 현재는 규칙 정확도 개선과 온라인 모드 확장을 함께 다룬다. 따라서 모든 작업은 단순 코드 수정이 아니라 다음 산출물 흐름을 기준으로 관리한다.

```text
1. 관련 문서 확인
2. 구현 계획서 작성 또는 기존 계획서 갱신
3. 구현
4. 테스트 계획서 작성
5. 테스트 수행
6. 최종 보고서 작성
```

위 단계 중 하나라도 생략하면 작업을 완료한 것으로 보지 않는다.

## 2. 관련 기준 문서

작업 전 필요한 문서를 확인한다.

공통 기준:

```text
catan_implementation_process_guideline.md
catan_detailed_rules.md
catan_project_rule_gap_analysis.md
```

온라인 모드 기준:

```text
docs/implementation_plans/2026-05-26_online-00-mvp-roadmap.md
docs/implementation_plans/2026-05-26_online-01-server-lan-room-plan.md
docs/implementation_plans/2026-05-26_online-02-ui-lobby-plan.md
docs/implementation_plans/2026-05-26_online-03-protocol-sync-plan.md
docs/implementation_plans/2026-05-26_online-04-game-engine-migration-plan.md
docs/implementation_plans/2026-05-26_online-05-reconnect-private-state-test-plan.md
```

온라인 모드에 대해서는 통합 메모인 `2026-05-26_online-mode-considerations.md`보다 위의 개별 계획서를 우선 기준으로 삼는다.

## 3. 절대 원칙

### 3.1 계획 없이 구현하지 않는다

코드를 수정하기 전에 반드시 구현 계획서를 작성하거나, 이미 존재하는 계획서를 이번 작업 범위에 맞게 갱신한다.

단순한 문구 수정이라도 다음은 기록해야 한다.

```text
무엇을 바꾸는가
왜 바꾸는가
어떤 파일이 영향을 받는가
어떻게 검증할 것인가
```

### 3.2 계획과 다르게 구현하면 계획서를 갱신한다

구현 중 계획과 다른 사실을 발견하면 즉시 계획서를 갱신한다.

예:

```text
예상한 함수가 실제로 다른 상태를 참조함
UI 수정 범위가 index.html뿐 아니라 styles.css까지 확장됨
온라인 상태가 game 객체만으로 부족하고 tiles/edges도 필요함
```

계획서가 현실과 어긋난 상태로 구현을 계속하지 않는다.

### 3.3 구현과 검증은 분리한다

구현이 끝났다는 것은 완료가 아니다. 구현 후에는 반드시 테스트 계획서를 작성하고, 그 계획에 따라 테스트한다.

### 3.4 테스트하지 못한 것은 숨기지 않는다

테스트를 수행하지 못한 경우에도 최종 보고서를 작성할 수 있다. 단, 반드시 다음을 명시한다.

```text
수행하지 못한 테스트
수행하지 못한 이유
남아 있는 위험
후속 검증 방법
```

### 3.5 최종 보고서는 작업의 일부다

최종 보고서는 선택 사항이 아니다. 모든 구현 작업은 최종 보고서까지 작성해야 끝난다.

## 4. 작업 유형 분류

작업을 시작할 때 먼저 유형을 정한다.

### 4.1 규칙 보완 작업

예:

```text
승리 조건 수정
최장 교역로 계산 수정
개발 카드 타이밍 수정
항구 교환 규칙 수정
```

필수 확인:

```text
catan_detailed_rules.md
catan_project_rule_gap_analysis.md
관련 기존 구현 계획서/최종 보고서
```

### 4.2 UI/UX 개선 작업

예:

```text
플레이어 패널 개선
대기실 UI 추가
버튼 상태 개선
모바일 레이아웃 보정
```

필수 확인:

```text
index.html
styles.css
script.js의 render 관련 함수
기존 스크린샷 또는 테스트 계획서
```

### 4.3 서버/온라인 작업

예:

```text
LAN 접속 지원
WebSocket 서버
방 만들기/참가
온라인 대기실
초기 배치 동기화
서버 권위 게임 명령
재접속
```

필수 확인:

```text
온라인 00~05 계획서
server.js
script.js의 게임 상태와 render 흐름
오프라인 모드 회귀 영향
```

### 4.4 테스트/문서 작업

예:

```text
테스트 계획서 보강
최종 보고서 작성
기존 계획서 비판적 검토
개발 가이드라인 갱신
```

코드 수정이 없더라도 변경 목적과 결과를 문서에 남긴다.

## 5. 구현 계획서 작성 규칙

권장 위치:

```text
docs/implementation_plans/
```

파일명:

```text
YYYY-MM-DD_short-feature-name.md
```

예:

```text
docs/implementation_plans/2026-05-26_online-01-server-lan-room-plan.md
docs/implementation_plans/2026-05-26_robber-victim-selection.md
```

### 5.1 구현 계획서 필수 항목

```text
작업 목적
관련 문서
현재 구현 상태
문제점
구현 범위
구현하지 않을 범위
수정 대상 파일
수정 대상 함수 또는 UI 영역
데이터 구조 변경 여부
UI 변경 여부
예상 영향 범위
단계별 구현 순서
검증 방법 초안
위험 요소
롤백 또는 복구 방법
```

### 5.2 큰 작업은 계획서를 쪼갠다

온라인 모드처럼 범위가 큰 작업은 하나의 거대한 계획서로 끝내지 않는다.

좋은 분리:

```text
00 MVP 로드맵
01 서버/LAN/방
02 UI/대기실
03 프로토콜/동기화
04 게임 엔진 이전
05 재접속/비공개 상태/테스트
```

나쁜 분리:

```text
온라인 모드 전체 구현 하나의 문서
```

### 5.3 MVP를 명확히 쓴다

큰 작업은 반드시 MVP와 후순위를 분리한다.

예:

```text
MVP:
- 방 만들기
- 방 참가
- 대기실 동기화
- 같은 보드 보기
- 초기 배치 동기화

후순위:
- 외부 인터넷 포트포워딩
- 방장 이전
- 게임 저장
- 채팅
```

## 6. 구현 규칙

### 6.1 기존 오프라인 모드를 보호한다

온라인 모드를 구현하더라도 오프라인 모드는 계속 동작해야 한다.

온라인 작업 시 항상 확인할 것:

```text
오프라인 게임 시작이 기존처럼 되는가
기존 버튼이 온라인 상태 변수에 영향받지 않는가
기존 render 흐름이 깨지지 않는가
```

### 6.2 서버 권위 원칙을 지킨다

온라인 모드에서는 서버가 게임 상태의 정답이다.

```text
클라이언트는 명령을 보낸다.
서버는 검증한다.
서버가 상태를 바꾼다.
클라이언트는 서버 state를 렌더링한다.
```

클라이언트가 확정하면 안 되는 것:

```text
현재 차례
주사위 결과
자원 변화
개발 카드 종류
건설 가능 여부
점수
승리 여부
```

### 6.3 온라인 구현 순서를 지킨다

온라인 작업은 다음 순서를 우선한다.

```text
MVP-A. 네트워크/방/대기실
MVP-B. 같은 보드 보기
MVP-C. 초기 배치 동기화
MVP-D. 주사위/턴 종료 동기화
MVP-E. 기본 건설 동기화
이후. 교환 -> 개발 카드 -> 7/강도
```

주사위/턴 종료를 초기 배치보다 먼저 서버화하지 않는다. 카탄은 초기 배치가 끝나야 실제 플레이 턴으로 진입한다.

### 6.4 상태 동기화 대상을 명확히 한다

온라인 상태는 `game`만으로 부족할 수 있다.

서버 `matchState`에는 최소한 다음을 고려한다.

```text
game
tiles
vertices
edges
harbors
robberTile
devDeck
pendingDiscards
pendingRobberVictims
pendingFreeRoads
```

### 6.5 프로토콜은 통일한다

온라인 WebSocket 요청은 다음 형식을 기준으로 한다.

```json
{
  "type": "command",
  "requestId": "r1",
  "roomId": "abc123",
  "playerId": "p_1",
  "playerToken": "secret",
  "payload": {
    "name": "rollDice"
  }
}
```

대기실과 게임 상태는 별도 이벤트를 남발하지 않고, 우선 `state.status`로 구분한다.

```text
state.status = lobby
state.status = playing
state.status = finished
```

### 6.6 변경 범위를 작게 유지한다

계획서에 없는 대규모 리팩터링을 하지 않는다.

허용되는 리팩터링:

```text
이번 구현을 위해 반드시 필요한 함수 추출
중복 제거보다 상태 분리를 위한 최소 변경
테스트 가능성을 높이는 작은 구조화
```

피해야 하는 리팩터링:

```text
전체 파일 스타일 변경
관련 없는 UI 재디자인
게임 규칙과 무관한 대규모 이름 변경
한 번에 전체 엔진 재작성
```

## 7. 테스트 계획서 작성 규칙

권장 위치:

```text
docs/test_plans/
```

파일명:

```text
YYYY-MM-DD_short-feature-name-test-plan.md
```

### 7.1 테스트 계획서 필수 항목

```text
테스트 대상
관련 구현 계획서
변경 파일 목록
테스트 환경
수동 테스트 항목
자동 테스트 항목
브라우저 검증 항목
규칙 검증 항목
회귀 테스트 항목
실패 시 확인할 로그 또는 상태
완료 기준
```

### 7.2 기본 테스트 유형

```text
정적 점검
브라우저 실행 확인
핵심 규칙 시나리오 테스트
UI 상호작용 테스트
회귀 테스트
```

### 7.3 온라인 작업 테스트 유형

온라인 작업에는 다음 테스트를 우선 포함한다.

```text
같은 PC의 다중 브라우저 테스트
일반 창 + 시크릿 창 테스트
LAN 다른 기기 접속 테스트
새로고침 재접속 테스트
중복 클릭 테스트
차례가 아닌 플레이어 명령 거부 테스트
상대 비공개 정보 노출 여부 테스트
```

LAN 테스트에서 확인할 것:

```text
서버가 0.0.0.0으로 실행되는가
Network URL이 출력되는가
친구 기기에서 http://방장PC_IP:포트 로 접속되는가
방장이 127.0.0.1 주소를 공유하지 않았는가
방화벽 문제가 안내되는가
```

## 8. 테스트 수행 규칙

### 8.1 테스트는 계획서 기준으로 수행한다

테스트 중 새로운 위험을 발견하면 테스트 계획서를 갱신한다.

### 8.2 브라우저 검증을 우선한다

이 프로젝트는 브라우저 게임이다. 단순 문법 확인만으로 테스트를 완료했다고 보지 않는다.

최소 확인:

```text
페이지가 열린다.
보드가 렌더링된다.
핵심 버튼이 동작한다.
콘솔 오류가 없다.
변경한 UI가 화면에서 확인된다.
```

### 8.3 온라인 기능은 다중 클라이언트로 검증한다

온라인 기능은 한 브라우저만으로 검증하지 않는다.

최소:

```text
브라우저 A: 방장
브라우저 B: 참가자
```

가능하면:

```text
브라우저 A: Chrome 일반 창
브라우저 B: Chrome 시크릿 창
브라우저 C: Edge 또는 다른 기기
```

## 9. 최종 보고서 작성 규칙

권장 위치:

```text
docs/final_reports/
```

파일명:

```text
YYYY-MM-DD_short-feature-name-final-report.md
```

### 9.1 최종 보고서 필수 항목

```text
작업명
작성일
관련 구현 계획서
관련 테스트 계획서
변경 파일 목록
구현 요약
규칙 또는 설계 반영 내용
테스트 결과
통과한 항목
실패한 항목
수행하지 못한 항목
남은 위험
후속 작업
최종 판단
```

최종 판단:

```text
완료
부분 완료
보류
실패
```

## 10. 문서 산출물 관리

디렉터리:

```text
docs/
  implementation_plans/
  test_plans/
  final_reports/
  known_issues/
```

파일명은 날짜와 짧은 작업명을 사용한다.

```text
YYYY-MM-DD_feature-name.md
```

같은 작업의 산출물은 이름을 맞춘다.

예:

```text
docs/implementation_plans/2026-05-26_online-01-server-lan-room-plan.md
docs/test_plans/2026-05-26_online-01-server-lan-room-test-plan.md
docs/final_reports/2026-05-26_online-01-server-lan-room-final-report.md
```

## 11. 작업 완료 체크리스트

작업 완료 전 반드시 확인한다.

```text
[ ] 관련 기준 문서를 확인했다.
[ ] 구현 계획서를 작성하거나 갱신했다.
[ ] 구현 계획서의 범위 안에서만 코드를 수정했다.
[ ] 계획 변경이 있었다면 문서에 반영했다.
[ ] 오프라인 모드 영향 여부를 확인했다.
[ ] 테스트 계획서를 작성했다.
[ ] 테스트 계획서에 따라 테스트를 수행했다.
[ ] 브라우저 실행 확인을 했다.
[ ] 실패하거나 수행하지 못한 테스트를 기록했다.
[ ] 최종 보고서를 작성했다.
[ ] 최종 판단을 명시했다.
```

온라인 작업일 경우 추가 확인:

```text
[ ] 서버/클라이언트 책임이 분리되어 있다.
[ ] 클라이언트가 게임 상태를 임의 확정하지 않는다.
[ ] 최소 2개 브라우저로 동기화를 확인했다.
[ ] 재접속 또는 연결 끊김 영향을 검토했다.
[ ] 비공개 정보 노출 여부를 검토했다.
```

## 12. 강제 준수 문구

앞으로 이 프로젝트에서 개발 작업을 진행할 때는 아래 문구를 기준으로 작업한다.

> 구현 계획서 없이 구현하지 않는다.  
> 계획과 다르면 계획서를 갱신한다.  
> 테스트 계획서 없이 테스트 완료로 보지 않는다.  
> 테스트 결과 없이 완료 보고하지 않는다.  
> 최종 보고서 없이 작업을 종료하지 않는다.

이 절차는 선택 사항이 아니라 필수 작업 절차다.
